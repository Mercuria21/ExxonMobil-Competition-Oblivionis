# ExxonMobil-Competition-Oblivionis
For the internal competition 

Training a linear regression model on weather station datasets to predict the PM2.5 level datapoint.

The model accepts the datasets given and loads it. It finds the relevant datapoints with a large correlation with the point we wish to predict. 

The training data and test data split is 70/30.

